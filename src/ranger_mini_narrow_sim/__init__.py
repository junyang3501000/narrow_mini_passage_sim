"""Ranger Mini 窄道控制与仿真包的核心 Python 模块。

具体功能分别位于参数、点云/TF、墙面估计、速度控制、里程、安全监督、任务状态机和
状态报告模块中；ros_node.py 只负责 ROS 通信与调用编排，此文件只声明包版本。
这些模块直接属于 ranger_mini_narrow_sim，不从相邻 ROS 包导入控制逻辑。
"""

# 使用显式版本号，方便以后打包或记录实验所用的软件版本。
__version__ = "0.1.0"
